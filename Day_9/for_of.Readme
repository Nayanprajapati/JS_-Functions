for (let number of numbers) → Iterates directly over values of the array (10, 20, 30, 40).
No need for indexing (i), unlike traditional for loops.
Best Used For: Iterating over arrays, strings, sets, and maps.